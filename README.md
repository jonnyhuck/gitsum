# gitsum
Produce a summary of a number of characteristics of a git repository

#### Notes
* This version was a test using the lower level interface instead of porcelain in case it was faster - it is, in fact, slower (~2.5x as long to analayse the [Paper2GIS](github.com/jonnyhuck/Paper2GIS) repo). The version in the [main](https://github.com/jonnyhuck/gitsum/tree/main) branch is therefore better.